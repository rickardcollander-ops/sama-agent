# SAMA 2.0 - Successifier Autonomous Marketing Agent
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0
anthropic>=0.49.0
supabase
httpx==0.26.0
python-dotenv==1.0.1
beautifulsoup4==4.12.3
lxml==5.1.0
apscheduler==3.10.4
PyJWT==2.9.0
resend>=2.0.0
PyNaCl>=1.5.0
sentry-sdk[fastapi]>=2.0.0
asyncpg>=0.29.0
redis>=5.0.0
stripe>=8.0.0
# Headless Chromium for the PDF render service used by the Apollo
# telemarketing flow in the dashboard. The browser binary is installed via
# `playwright install --with-deps chromium` in the Dockerfile.
playwright==1.48.0
